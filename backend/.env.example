ANTHROPIC_API_KEY=your_api_key_here

# Injected automatically by Railway PostgreSQL plugin — do not set manually
# DATABASE_URL=

PORT=3001

# Uncomment if your local network uses SSL inspection (corporate proxy)
# NODE_TLS_REJECT_UNAUTHORIZED=0
